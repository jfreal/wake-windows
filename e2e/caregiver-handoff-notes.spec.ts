// @test:caregiver-handoff-notes
import { test, expect } from '@playwright/test';

// Feature: Caregiver Handoff Notes & Summary (Sharing & Collaboration) — status: Proposed (not built yet).
// Placeholder kept skipped so the E2E matrix covers every feature.
// When this ships: tag the code with `// @doc:caregiver-handoff-notes`, then replace test.skip
// with real assertions and run `npm run test:e2e`.
test.describe('Caregiver Handoff Notes & Summary [@feature:caregiver-handoff-notes]', () => {
  test.skip('TODO: implement E2E once caregiver-handoff-notes is built', async ({ page }) => {
    await page.goto('/');
  });
});
