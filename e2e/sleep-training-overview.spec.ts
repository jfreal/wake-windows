// @test:sleep-training-overview
import { test, expect } from '@playwright/test';

// Feature: Sleep-Training Methods Overview (Neutral) (Guidance & Credibility) — status: Proposed (not built yet).
// Placeholder kept skipped so the E2E matrix covers every feature.
// When this ships: tag the code with `// @doc:sleep-training-overview`, then replace test.skip
// with real assertions and run `npm run test:e2e`.
test.describe('Sleep-Training Methods Overview (Neutral) [@feature:sleep-training-overview]', () => {
  test.skip('TODO: implement E2E once sleep-training-overview is built', async ({ page }) => {
    await page.goto('/');
  });
});
